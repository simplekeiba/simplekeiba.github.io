# AGENTS.md

このリポジトリで作業するエージェントは、以下のルールを常に守ること。

## プロジェクト

ポッドキャスト番組「シンプルKEIBA～難しくない競馬ラジオ～」の公式サイト。

**最優先ゴールは新規リスナーの獲得。** サイトの成功とは「訪問者が配信アプリで1話目を再生し、フォローすること」である。滞在時間を延ばすことは目的ではない。**速やかにSpotify / Apple へ送り出すことが正解。**

迷ったら次の2つで判断する。

1. 競馬をよく知らない人が、これを見て「自分向けだ」と思うか
2. 再生ボタンまでのクリック数が減るか

詳細仕様は `docs/spec.md` にある。**作業開始前に必ず全文を読むこと。** 本ファイルと矛盾する場合は `docs/spec.md` を優先する。

## スタック

- Astro 5.x / `output: 'static'`
- Tailwind CSS v4（トークンは `src/styles/theme.css` の `@theme` に集約）
- TypeScript（`strict: true`）
- 画像は `astro:assets` 経由でのみ扱う

## 絶対に守ること

1. **`docs/spec.md` 5.3 に定義されたカラートークン以外の色を使わない。** 新しい色が必要になったら実装せず、まず確認を求める
2. **ダミーテキスト（Lorem ipsum、架空のエピソードタイトル、架空の数値）を一切生成しない。** 未確定箇所は `{{TODO: 内容}}` と明示し、完了報告に全件列挙する
3. **エピソードのURL（slug）は `ep-{話数}` 形式で固定。** 一度決めた形式を後から変更しない。検索評価と被リンクを失う
4. **全ページに `title` / `meta description` / OGP / 構造化データを実装する。** SEOは本プロジェク
<truncated 2485 bytes>
位置で到達可能に保つ
- ショーノート本文は行長40〜45文字、行間1.9を確保する

## 実行環境の制約

このWindows環境ではPowerShellのスクリプト実行が無効化されているため、
npm や npx を直接実行すると UnauthorizedAccess エラーで失敗します。

今後このプロジェクトで npm / npx を実行する際は、必ず
cmd /c npm ... / cmd /c npx ... の形式で実行してください。

## コーディング規約

- コンポーネントは PascalCase（`EpisodeRow.astro`）
- ユーティリティは camelCase（`fetchFeed.ts`）
- 1コンポーネント150行を超えたら分割を検討する
- インラインスタイル禁止。Tailwindで表現できない場合のみ `<style>` を使い、理由をコメントで残す
- `any` を使わない
- RSSデータは必ず型定義と正規化を通してからコンポーネントへ渡す

## 作業の進め方

- **一度に全ページを実装しない。** 1タスク＝1ページまたは1コンポーネント単位に分割する
- 各タスク完了時、ブラウザで表示を確認し、モバイル（375px）とデスクトップ（1440px）両方のスクリーンショットを Artifact として残す
- 仕様にない機能を勝手に追加しない。提案は実装せず文章で行う

## 品質チェック（各タスクの完了条件）

- [ ] `npm run build` が警告なしで通る
- [ ] TypeScriptエラーがゼロ
- [ ] モバイル375pxで横スクロールが発生しない
- [ ] Tabキーのみで全リンク・ボタンに到達でき、フォーカスが視認できる
- [ ] 該当ページのメタ情報と構造化データが実装済み
- [ ] `{{TODO}}` の残存箇所をすべて報告した

## 禁止事項

- `docs/spec.md` および本ファイルを、確認なしに書き換えない
- 公開済みURLの構造を変更しない
- 新規パッケージの追加は理由を添えて事前確認する